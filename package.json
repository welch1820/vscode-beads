{
  "name": "vscode-beads",
  "displayName": "Beads",
  "description": "VS Code extension for managing Beads issues",
  "version": "0.12.0",
  "publisher": "planet57",
  "license": "Apache-2.0",
  "icon": "resources/icon.png",
  "files": [
    "LICENSE",
    "CHANGELOG.md",
    "dist/**",
    "resources/**",
    "docs/images/**"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/welch1820/vscode-beads"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "keywords": [
    "beads",
    "issues",
    "project management",
    "dashboard"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "beads",
          "title": "Beads",
          "icon": "resources/beads-icon.svg"
        }
      ]
    },
    "views": {
      "beads": [
        {
          "type": "webview",
          "id": "beadsDashboard",
          "name": "Dashboard",
          "visibility": "hidden"
        },
        {
          "type": "webview",
          "id": "beadsPanel",
          "name": "Issues",
          "visibility": "visible"
        },
        {
          "type": "webview",
          "id": "beadsDetails",
          "name": "Details",
          "visibility": "visible"
        },
        {
          "type": "webview",
          "id": "beadsGraph",
          "name": "Graph",
          "visibility": "hidden"
        }
      ]
    },
    "commands": [
      {
        "command": "beads.switchProject",
        "title": "Beads: Switch Project",
        "icon": "$(folder)"
      },
      {
        "command": "beads.openBeadsPanel",
        "title": "Beads: Open Issues Panel",
        "icon": "$(list-unordered)"
      },
      {
        "command": "beads.openBeadDetails",
        "title": "Beads: Open Issue Details",
        "icon": "$(file-text)"
      },
      {
        "command": "beads.refresh",
        "title": "Beads: Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "beads.createBead",
        "title": "Beads: Create New Issue",
        "icon": "$(add)"
      },
      {
        "command": "beads.showStatusMenu",
        "title": "Beads: Status Menu",
        "icon": "$(server-process)"
      },
      {
        "command": "beads.copyBeadId",
        "title": "Beads: Copy Issue ID",
        "icon": "$(copy)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "beads.refresh",
          "when": "view == beadsPanel",
          "group": "navigation@1"
        },
        {
          "command": "beads.createBead",
          "when": "view == beadsPanel",
          "group": "navigation@2"
        },
        {
          "command": "beads.copyBeadId",
          "when": "view == beadsDetails && beads.hasSelectedBead",
          "group": "navigation@1"
        },
        {
          "command": "beads.refresh",
          "when": "view == beadsGraph",
          "group": "navigation@1"
        }
      ]
    },
    "configuration": {
      "title": "Beads",
      "properties": {
        "beads.pathToBd": {
          "type": "string",
          "default": "bd",
          "description": "Path to the bd CLI executable"
        },
        "beads.refreshInterval": {
          "type": "number",
          "default": 30000,
          "description": "Auto-refresh interval in milliseconds (0 to disable)"
        },
        "beads.renderMarkdown": {
          "type": "boolean",
          "default": true,
          "description": "Render markdown in description, design notes, and other text fields"
        },
        "beads.userId": {
          "type": "string",
          "default": "",
          "description": "Your user ID for 'Assign to me'. Defaults to $USER if not set."
        },
        "beads.tooltipHoverDelay": {
          "type": "number",
          "default": 1000,
          "minimum": 0,
          "description": "Delay in milliseconds before showing bead description tooltip on hover. Set to 0 to disable tooltips."
        },
        "beads.gitlab.url": {
          "type": "string",
          "default": "",
          "description": "GitLab instance URL (e.g., https://gitlab.com). Leave empty to skip GitLab team member discovery."
        },
        "beads.gitlab.token": {
          "type": "string",
          "default": "",
          "description": "GitLab personal access token with read_api scope. Used to fetch project members for the assignee dropdown."
        },
        "beads.bugzilla.url": {
          "type": "string",
          "default": "",
          "description": "Bugzilla server URL (e.g., https://bugzilla.example.com). Leave empty to disable Bugzilla integration."
        },
        "beads.bugzilla.apiKey": {
          "type": "string",
          "default": "",
          "description": "Bugzilla API key for authentication. Generate one in Bugzilla Preferences > API Keys."
        },
        "beads.bugzilla.username": {
          "type": "string",
          "default": "",
          "description": "Your Bugzilla email/username. Used to fetch bugs assigned to you."
        }
      }
    }
  },
  "scripts": {
    "stamp": "echo 'export const BUILD_TIMESTAMP = \"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'\";' > src/build-stamp.ts",
    "compile": "bun run stamp && bun run compile:extension && bun run compile:webview",
    "compile:quiet": "bun run stamp && bun run compile:extension:quiet && bun run compile:webview:quiet",
    "compile:extension": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --target=node18",
    "compile:extension:quiet": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --target=node18 --log-level=error",
    "compile:webview": "esbuild src/webview/index.tsx --bundle --outfile=dist/webview/main.js --format=iife --platform=browser --target=es2020 --loader:.tsx=tsx --loader:.ts=ts --loader:.css=css --loader:.svg=text --define:process.env.NODE_ENV=\\\"production\\\"",
    "compile:webview:quiet": "esbuild src/webview/index.tsx --bundle --outfile=dist/webview/main.js --format=iife --platform=browser --target=es2020 --loader:.tsx=tsx --loader:.ts=ts --loader:.css=css --loader:.svg=text --define:process.env.NODE_ENV=\\\"production\\\" --log-level=error",
    "watch": "concurrently \"bun run watch:extension\" \"bun run watch:webview\"",
    "watch:extension": "bun run compile:extension -- --watch --sourcemap",
    "watch:webview": "bun run compile:webview -- --watch --sourcemap",
    "lint": "eslint src --ext ts,tsx",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "vscode:prepublish": "bun run compile",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "concurrently": "^8.2.2",
    "esbuild": "^0.19.10",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@tanstack/react-table": "^8.21.3",
    "elkjs": "^0.11.1",
    "lucide-react": "^0.563.0",
    "marked": "^17.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
